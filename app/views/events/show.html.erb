
<h1><%= @location.name %></h1>
<%= image_tag(@location.image) %>

  <ul>
    <li> <%= @event.name %></li>
    <li> <%= @event.date %> </li>
  </ul>
  <%= link_to 'Edit', edit_location_event_path %> |
   <%= link_to 'Delete' , location_event_path, method: :delete, data: {confirm: "Are you sure you want to delete #{@event.name}?"} %> |
   <%= link_to 'Back', location_events_path %>
